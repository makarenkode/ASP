@model PagedList.IPagedList<AutomobileEnterprise.Models.Route.Departure>
@using PagedList.Mvc
@{
    ViewBag.Title = "Departures";
}

<h2>Departures</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First().Car.Model)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Date)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Car.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.DepartureId }) |
                @Html.ActionLink("Details", "Details", new { id = item.DepartureId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.DepartureId })
            </td>
        </tr>
    }

</table>
Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
@Html.PagedListPager(Model, PageNum =>Url.Action("Index",
    new { PageNum }))